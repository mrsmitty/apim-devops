parameters:
  - name: salutation
    type: string
jobs:
  - job:
    pool:
      vmImage: ubuntu-latest
    steps:
      - task: ShellScript@2
        inputs:
          scriptPath: run.sh
          args: ${{ parameters.salutation }}